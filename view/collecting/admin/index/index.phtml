<?php
echo $this->pageTitle('Collecting');
?>

<div id="page-actions">
    <?php echo $this->hyperlink($this->translate('Add new form'), $this->url(null, ['action' => 'add'], true), ['class' => 'button']); ?>
</div>
<nav class="section-nav">
    <?php echo $this->navigation('Zend\Navigation\Collecting')->menu(); ?>
</nav>

<table class="tablesaw">
    <thead>
    <tr>
        <th>Label</th>
        <th>Owner</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach ($collectingForms as $collectingForm): ?>
    <?php
    $owner = $collectingForm->owner();
    $ownerName = $owner ? $owner->name() : null;
    ?>
    <tr>
        <td>
            <?php echo $collectingForm->label(); ?>
            <ul class="actions">
                <li><?php echo $this->hyperlink(null, $this->url('admin/site/slug/collecting/id', ['action' => 'edit', 'id' => $collectingForm->id()], true), ['class' => 'o-icon-edit', 'aria-label' => 'Edit', 'title' => 'Edit']); ?></li>
                <li><?php echo $this->hyperlink(null, '#', ['class' => 'o-icon-delete', 'aria-label' => 'Delete', 'title' => 'Delete']); ?></li>
            </ul>
        </td>
        <td><?php echo $ownerName; ?></td>
    </tr>
    <?php endforeach; ?>
    </tbody>
</table>
